#pragma once

    class Flags
    {
        public:
            // zero
            bool z;

            // negative
            bool n;

            // halfcarry
            bool h;

            // carry
            bool c;

            Flags();
            ~Flags();
    };
